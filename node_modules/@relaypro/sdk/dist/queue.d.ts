interface Params {
    max?: number;
}
declare type work = {
    (): PromiseLike<void>;
};
export default class Queue {
    private max;
    private numActive;
    private queue;
    constructor(params?: Params);
    private next;
    private execute;
    enqueue(fn: work): void;
}
export {};
